import { Nunito, Karla } from 'next/font/google';

export const karla = Karla({
  subsets: ['latin'],
  display: 'swap',
});

export const nunito = Nunito({
  subsets: ['latin'],
  display: 'swap',
});
